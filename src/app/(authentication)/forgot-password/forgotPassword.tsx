'use client';

import React, { ReactNode, useState } from 'react';
export default function AuthenticationLayout({
  children,
}: {children: ReactNode}){
  <html lang='en'>
      {children}
  </html>
}

